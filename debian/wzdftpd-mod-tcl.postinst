#!/bin/sh

set -e

CONFIG=/etc/wzdftpd/wzd.cfg

if [ "$1" != "configure" ]; then
	exit 0
fi

if [ -f $CONFIG ]; then
	if ! grep -q "^module[[:space:]]*=[[:space:]]*.*libwzd_tcl.so" $CONFIG
	then
	  echo "module = /usr/lib/wzdftpd/modules/libwzd_tcl.so" >> $CONFIG

	  if ! grep -q "^-site_tcl.*" $CONFIG
	  then
	    echo "-site_tcl = +O" >> $CONFIG
	  fi
	fi
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

